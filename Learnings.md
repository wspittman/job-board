# Learnings

- `dry-utils-cosmosdb` now supports `mockDBOptions` on `connectDB`, including fixture `data` and query interception with `queries` matchers. This enables backend startup in environments without a Cosmos DB emulator.
