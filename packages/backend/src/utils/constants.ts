export const SEC_PER_DAY = 24 * 60 * 60;
export const MS_PER_DAY = SEC_PER_DAY * 1000;
export const JOB_EXPIRY_DAYS = 90;
export const JOB_EXPIRY_SEC = JOB_EXPIRY_DAYS * SEC_PER_DAY;
export const JOB_EXPIRY_MS = JOB_EXPIRY_DAYS * MS_PER_DAY;
